# task-3004 handoff: シードデータ投入スクリプト

## Spec Alignment

spec.md ゴール「ローカル完結・非エンジニア運用」および受け入れ条件 (§4 認証/タイムカード/シフト/店舗区別) を開発・デモで検証可能にするためのサンプルデータを投入する。phase-3 data layer チケットの最後として、migrate 済み DB に 2 店舗・管理者/店長/スタッフ・過去打刻・翌週シフトを流し込む。

## Phase

phase-3 / task-3004 (backend, seed script)

## Completed

- `scripts/seed.ts` 新規作成
  - `better-sqlite3` + 既存 `createSqlite` / `resolveDbPath` を再利用
  - FK 順を守って `DELETE FROM` + `sqlite_sequence` リセットで idempotent に動作
  - bcrypt で password / PIN をハッシュ化 (rounds=10)
  - 2 店舗 (`雀庵` / `雀庵はなれ`) 投入、営業 17:00-23:30、定休日付き
  - employees 10 名:
    - admin: 店主 雀子 (login_id=`oyakata`, password=`hanare2026`, pin=`9999`)
    - manager: 雀庵店長 (pin=`1111`) / はなれ店長 (pin=`2222`) (各 loginId/password あり)
    - staff: 各店舗 3 名 (pin 連番 1001-1003 / 2001-2003) + 兼務 1 名 (pin=`3001`)
  - `employee_stores` に所属/兼務 (is_primary フラグ) を設定
  - 直近 14 日のうち 5 日分 × 4 打刻 × staff 7 名 = 140 件の `time_punches` (clock_in → break_start → break_end → clock_out)
  - 翌週月曜から 6 日分 × 3 名 = 18 件の `shifts` を draft/published 混在で投入
  - 全体を 1 トランザクションでラップ、ログで各件数を出力
- `npm run migrate && npm run seed` の一連フローで `data/hanare.db` に投入できることを確認
- 再実行 (idempotent) 動作確認: 2 回連続実行して件数に差分なし
- `npx tsc --noEmit`: pass
- `npx biome check scripts/seed.ts`: pass (autoformat 1 回適用後)
- DB 実体サニティ確認: `sqlite3` で stores 2 / role 別 employees (admin=1, manager=2, staff=7) / shifts 18 (draft=9, published=9) / time_punches=140 を確認

## In Progress

なし

## Blocked

なし

## Tests Run

- `npm run migrate` → applied 0000_init.sql
- `npm run seed` (2 回連続) → 両回とも stores=2 employees=10 employee_stores=12 time_punches=140 shifts=18
- `npx tsc --noEmit` → exit 0
- `npx biome check scripts/seed.ts` → no issues
- `sqlite3` による直接 SELECT → 期待通り

## Known Gaps

- Orchestrator 指示には "admin/admin0000" とあったが、ticket-3004 本文で `oyakata/hanare2026` が明示されていたためチケットを優先した。別資格情報が必要なら generator 経由ではなく orchestrator で更新するのが安全。
- 過去打刻は 5 日分で full 20:00-20:30 の休憩 + 17:00 出勤 23:30 退勤の単純パターン。日またぎ (深夜越え) ケースはサンプルに含めていない → 日またぎ集計テストが必要な場合は後続チケットで追加推奨。
- `shift_requests` / `correction_requests` / `audit_logs` / `work_days` は空のまま (チケットのスコープ外)。
- `DELETE FROM` で既存データを一律消去する方針なので、本番データを消す事故を避けたい場合は環境変数チェック等を後続で入れる余地あり。

## Next Step

1. Orchestrator 側で `/verify tasks/phase-3/task-3004.md` を実行し done_when を確認する
2. phase-3 全体の evaluator を実行し phase 3 を closeout する
3. phase-4 (auth / API 層) に着手する際、このシードの資格情報 (`oyakata` / `hanare2026`, manager の loginId/password, staff PIN) をそのまま利用できる

## Key Files

- `/Users/kzt/Desktop/project-d/hanare-timecard/scripts/seed.ts` (新規)
- `/Users/kzt/Desktop/project-d/hanare-timecard/package.json` (`npm run seed` はすでに定義済、変更不要)
- `/Users/kzt/Desktop/project-d/hanare-timecard/data/hanare.db` (実体。seed 済)
