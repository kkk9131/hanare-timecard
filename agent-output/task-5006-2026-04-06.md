# task-5006 シフト編成画面 A02 Handoff

## Spec Alignment

spec.md §3.3「シフト管理ダッシュボード」および §4 受け入れ条件「シフト」（週ビュー編成・公開、ドラフト/公開区別、重複検出、人員不足ハイライト、シフト希望閲覧）に対応。店長が 5 分以内に 1 週間分シフトを編成・公開できる導線を実現する。

## Phase

Phase 5 / Implement (generator)

## Completed

- `src/client/api/admin.ts` に Shift mutation 関数を追加
  - `createShift` (POST /api/shifts)
  - `updateShift` (PATCH /api/shifts/:id)
  - `deleteShift` (DELETE /api/shifts/:id)
  - `publishShifts` (POST /api/shifts/publish)
  - `getShiftConflicts` (GET /api/shifts/conflicts)
  - `listShiftRequests` (GET /api/shift-requests)
  - 対応する zod レスポンススキーマ／TS 型 (`ShiftConflictReport`, `ShiftRequest` 等)
- `src/client/routes/AdminShifts.tsx` を新規作成
  - 週ビュー / 月ビュー切替 (週=月曜起算 7 日、月=月初〜月末)
  - 前/今日/翌 ナビゲーション (週 / 月単位)
  - 店舗タブ (StoreSwitcher、`includeAll=false` で必ず単一店舗を指定)
  - 縦軸=従業員 / 横軸=日付の CSS Grid（横スクロール対応、左列・上行 sticky）
  - 空セルクリック → 追加モーダル → POST /api/shifts (draft で作成)
  - 既存セルクリック → 編集モーダル → PATCH or DELETE
  - 「この週/月を公開する」ボタン → 確認ダイアログ「{N} 件のシフトを公開します。よろしいですか？」 → POST /api/shifts/publish
  - ドラフト = 点線 + 金茶、公開済 = 実線 + 朱赤 で視覚区別、文字ラベル併記
  - GET /api/shifts/conflicts の結果から人員不足日（understaffed）を列ヘッダ警告アイコン + セル背景でハイライト
  - 重複検出: 入力時はクライアントでバリデーション、サーバ 409 を `ApiError` で受けて朱色エラー表示
  - シフト希望（GET /api/shift-requests）を `<details>` 折り畳みパネルで閲覧
  - 削除前確認、Esc キー / 背景クリックでモーダル閉じる
- `src/client/routes/AdminShifts.css` を新規作成
  - 和モダンスタイル: tokens.css の washi/sumi/kincha/shu 変数のみで構成
  - 明朝見出し + ゴシック本文、tabular-nums の時刻表示
  - WCAG focus ring (kincha-400 outline)
- `src/client/routes/admin.tsx` の placeholder export を新ページに置換 (`export { AdminShiftsPage } from "./AdminShifts"`)

## In Progress

なし

## Blocked

なし

## Tests Run

- `npx tsc --noEmit` → 自分の対象ファイル (AdminShifts.tsx / admin.ts / admin.tsx / AdminShifts.css) でエラー 0
  - `tests/unit/*.http.verify.test.ts` と `MeDashboard.tsx` に既存エラーが残るがスコープ外（task-5006 とは無関係）
- `npx biome check src/client/routes/AdminShifts.tsx src/client/routes/AdminShifts.css src/client/api/admin.ts src/client/routes/admin.tsx` → エラー 0 警告 0
- `npx vite build` → 成功
  ```
  dist/client/assets/index-CCvVqL_Y.js   452.53 kB │ gzip: 131.09 kB
  ✓ built in 132ms
  ```
- 手動: ブラウザ起動による実機操作確認は未実施（サーバ起動を伴う E2E は Evaluate フェーズで ui-reviewer / evaluator が担当する想定）

## Known Gaps

- ドラッグ操作（dnd-kit）は任意項目のため未実装。クリック追加 / 編集 / 削除のみ
- 月ビューでは 1 セルが小さくなる対策として chip サイズを縮小しているが、6 週分（最大 31 日）でも横スクロールが必要なケースあり
- シフト希望は閲覧のみで「希望から空セルへ反映」ワンクリック導線は未実装（チケット範囲外）
- 店舗未選択時（store API 失敗時）のフォールバック UI は最小限
- 公開済み chip は編集（時刻変更）はできるが、UI 上は draft と同様にクリックで開く設計。公開済みの削除は不可（DELETE が 409 を返す前提を文言で明示）

## Next Step

1. Orchestrator が `/verify task-5006` を実行し done_when を検証する
2. fail があれば `/fix` または再 generator 起動で対応
3. ui-reviewer 系チェック（視覚的品質）は Evaluate フェーズで実施

## 触ったファイル

- `src/client/api/admin.ts` (Shift mutation 群を追加)
- `src/client/routes/AdminShifts.tsx` (新規)
- `src/client/routes/AdminShifts.css` (新規)
- `src/client/routes/admin.tsx` (placeholder 置換)
