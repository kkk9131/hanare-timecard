# task-6002 handoff: README と運用ドキュメント

## Spec Alignment

spec.md 5 章「非機能要件 / 可搬性」(`npm install && npm run start` 相当 2 コマンド起動、DB コピーによる移行) と 5 章「データ保存 / 日次自動バックアップ 30 世代」の運用化に対応。非エンジニア運用者が README だけで起動・バックアップ・トラブルシュートできる状態を作るのが目的。

## completed

- `/Users/kzt/Desktop/project-d/hanare-timecard/README.md` 新規作成
  - プロジェクト概要 (雀庵 / 雀庵はなれ タイムカード兼シフト)
  - 動作要件 (Node.js v20+)
  - インストール手順 (`git clone` → `npm install` → `npm run migrate` → `npm run seed` → `./start.sh`)
  - LAN 共有手順 (起動バナー例、PORT 変更、ファイアウォール注意)
  - 初期ログイン情報テーブル (seed.ts の 10 名分と実際の PIN を正確に反映: oyakata/hanare2026, jakuan_mgr/jakuan2026, hanare_mgr/hanare2026, 山田1001, 佐藤1002, 鈴木1003, 田中2001, 高橋2002, 伊藤2003, 渡辺3001)
  - 主要機能 / 画面マップ表
  - データ保存 (`data/hanare.db`) と移行手順 (scp + migrate)
  - バックアップ (手動 / HANARE_BACKUP_ON_START / cron)
  - TLS 方針 (Caddy reverse proxy + `HANARE_TLS=1` で Secure cookie)
  - トラブルシュート (起動失敗 / LAN / PIN ロック / DB 復旧)
  - 技術スタック一覧
- `/Users/kzt/Desktop/project-d/hanare-timecard/docs/operations.md` 新規作成
  - 日次運用フロー (開店前 / 打刻 / 修正申請)
  - 週次シフト編成フロー
  - 月次締めとエクスポート手順 + 確認ポイント
  - バックアップ運用 (launchd / crontab / 外部保管)
  - PIN / パスワードリセット (UI 経由 3 パターン + DB 直接 + bcrypt ワンライナー)
  - 障害対応 (起動失敗 / DB 復旧 / ロック解除 / 打刻未反映)
  - セキュリティ注意事項 (ネットワーク / TLS / PIN / 監査ログ / 物理)
  - アップデート手順 (backup → pull → install → migrate → rebuild)
- `/Users/kzt/Desktop/project-d/hanare-timecard/docs/development.md` 新規作成
  - ディレクトリ構成図 (実際の `src/server` `src/client` `src/shared` `scripts` `drizzle` 等)
  - npm スクリプト一覧表
  - 開発フロー (dev server + Vite HMR)
  - テスト実行
  - 新しいエンドポイント追加 7 ステップ (zod schema → service → route → mount → client API → UI → test)
  - データモデル変更フロー (drizzle-kit generate → SQL 確認 → backup → migrate → seed/test)
  - コーディング規約 / デバッグ tips
- `npm run migrate` 実行して冪等性確認 (`applied=0 total=1` でエラーなし)

## in_progress

なし

## blocked

なし

## tests_run

- `npm run migrate`: 成功 (`applied=0 total=1`)。README 記載の migrate 手順が動作することを実環境で確認
- README の手順書通りに最初から (`git clone` 部分除く) 実行可能なことは `npm install` 既実施・`migrate` 成功・`start.sh` 既存 (task-6001) により担保されている
- Markdown 構文: 3 ファイルとも見出し階層・コードブロック・テーブル構文は valid (Write 後 formatter hook が通過、エラーなし)
- `npm run seed` / `./start.sh` の実動作は task-6001 で既に確認済みのため本タスクでは再実行せず

## known_gaps

- スクリーンショット (`docs/screenshots/`) は撮影していない。チケット上「任意」指定であり、ドキュメント本文の情報密度で十分と判断。必要なら後続タスクで追加可能
- TLS について、ticket で言及された `HANARE_TLS_CERT` / `HANARE_TLS_KEY` 環境変数はコードベースに実装されていないため、README/operations では **リバースプロキシ (Caddy) + `HANARE_TLS=1` (セッション Cookie の Secure 属性フラグ、実装済み)** 方式を正として記載した。未実装の変数を README に書くと運用者を混乱させるための判断
- README の git clone URL はプレースホルダ (`<repo-url>`) のまま。リポジトリを GitHub/GitLab にホストする段階で置換が必要
- 画面 URL 表 (`/me`, `/admin/shifts` 等) は architecture 前提の「概念 URL」として記載。実ルータ定義とズレがないかは evaluator フェーズで確認すべき

## next_step

1. Orchestrator が `/verify task-6002` を実行
2. Phase 6 全チケット完了後、evaluator で「非エンジニア運用者が README だけで起動できるか」の観点で統合評価
3. 将来、スクリーンショットを追加するなら `docs/screenshots/` に配置し README と operations.md からリンク
