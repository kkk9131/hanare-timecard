# task-5008 done_when

- `/admin/exports` の「今月をエクスポート」ボタン 1 クリックで xlsx がダウンロードされる
- カスタム期間 + CSV ボタンも動作する
- ダウンロード完了トーストが出る
- `/admin/audit` で監査ログが時系列降順で表示される
- フィルタ (期間/actor/action) で絞り込める
- ページネーションが動作する
- 削除ボタン UI が存在しない (read only)
- staff で URL 直叩きすると 403/リダイレクト
