# task-5001 done_when

- `npm run dev` でサーバ + Vite が起動し http://localhost:5173 でアクセス可
- ui-design.md の全画面パス (K01-K04, E01-E05, A00-A07) に遷移するとプレースホルダが表示される
- 未認証で `/admin` を開くと `/admin/login` にリダイレクトされる
- 未認証で `/me` を開くと `/` にリダイレクトされる
- TanStack Query の DevTools が dev でのみ表示される (任意)
- `vite build` がエラーなく完走
