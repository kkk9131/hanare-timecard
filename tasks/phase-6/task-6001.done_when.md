# task-6001 done_when

- `tsx scripts/backup.ts` 実行で `data/backups/hanare-YYYYMMDD-HHmm.db` が生成される
- 32 個目のバックアップを実行すると最古が削除され常に 30 世代以下になる (テスト用に小さい maxGen で確認可)
- `./start.sh` がエラーなく起動する (実行権限あり)
- `./start.sh` で listen 中に `curl /api/system/health` が `ok` を返す
- admin で `POST /api/system/backup` がバックアップ生成 + 200 を返す
- backup.cron.example が存在し launchd または crontab エントリ例を含む
