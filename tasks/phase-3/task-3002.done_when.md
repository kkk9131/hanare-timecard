# task-3002 done_when

- `src/server/db/schema.ts` に 10 テーブルが定義されている
- `npx drizzle-kit generate` がエラーなく完走し `drizzle/0000_init.sql` が生成される
- `npm run migrate` 実行で `data/hanare.db` が生成される
- sqlite3 CLI or better-sqlite3 で `.tables` 相当を実行すると 10 テーブル + `__drizzle_migrations` が表示される
- 2 回目の migrate がエラーなく no-op で完了する
- 各テーブルに data-model.md 記載のインデックスが存在する (`PRAGMA index_list` で確認)
