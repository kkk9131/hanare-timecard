# task-3001 done_when

- `package.json` が存在し scripts に dev/build/start/migrate/seed/lint/test を含む
- `npm install` が成功する
- `npx tsc --noEmit` がエラーなし
- `biome check .` が pass (フォーマット除外設定 OK)
- `vite build` が成功し `dist/client/index.html` が生成される
- `tsx src/server/index.ts` が boot ログを出して終了 or 起動する
- ディレクトリ `src/server`, `src/client`, `src/shared`, `scripts`, `data`, `drizzle`, `tests` が存在
- `.gitignore` に `node_modules`, `dist`, `data/*.db`, `data/backups` が含まれる
