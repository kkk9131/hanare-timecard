# task-3004 done_when

- `npm run seed` がエラーなく完走する
- 完走後 `data/hanare.db` の各テーブルに以下のレコード数が存在:
  - stores: 2
  - employees: >= 9
  - employee_stores: >= 9
  - time_punches: > 0
  - shifts: > 0
- admin ユーザー `oyakata` の password_hash と pin_hash が bcrypt 形式 (`$2b$` 始まり)
- 2 回目の seed 実行でも重複エラーなく成功する (冪等)
