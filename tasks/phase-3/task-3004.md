---
id: task-3004
phase: 3
name: シードデータ投入
type: backend
depends_on: [task-3002, task-3003]
---

# シードデータ投入

## 背景

開発・デモ用に最低限のサンプルデータを投入する `scripts/seed.ts` を作成する。

## 追加するファイル

- `scripts/seed.ts`

## 投入内容

- stores: `雀庵 (jakuan)`, `雀庵はなれ (hanare)` 2 件
- employees:
  - admin 1 名 `店主 雀子` (login_id=`oyakata`, password=`hanare2026`, pin=`9999`, role=admin)
  - manager 2 名 (各店長), pin=`1111`/`2222`
  - staff 6 名 (各店舗 3 名 + 兼務 1 名), pin は連番
- employee_stores: 所属関係を設定
- 直近 14 日分のサンプル打刻 (1 名 5 日 × 4 打刻) を `time_punches` に投入
- 翌週分のシフト draft + published 混在を `shifts` に数件投入

## 実装方針

- bcrypt で PIN/PW をハッシュ化
- 既存 DB がある場合は先に `DELETE FROM` で全テーブルクリア (順序注意)
- `npm run seed` で実行可能に
- ログ出力で投入件数を表示

## 参考

- agent-docs/data-model.md
