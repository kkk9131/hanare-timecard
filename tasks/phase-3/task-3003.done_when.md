# task-3003 done_when

- `src/shared/types.ts` と `src/shared/schemas.ts` が存在
- api-spec.md の全エンドポイントに対応する zod スキーマが定義済み
- `npx tsc --noEmit` がエラーなし
- vitest で `pinLoginSchema.safeParse({employee_id:1, pin:'1234'})` が success、`{pin:'12'}` が fail を返す簡易テストが書かれ pass する
