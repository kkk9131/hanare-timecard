# task-4006 done_when

- `curl -OJ http://localhost:3000/api/exports/xlsx?from=YYYY-MM-01&to=YYYY-MM-30` で xlsx ファイルがダウンロードされる
- ダウンロードした xlsx を Excel for Mac (or Numbers) で開いて文字化けなく表示される (手動確認)
- CSV のバイト列先頭が `EF BB BF` (BOM)、改行が `\r\n` であること (`xxd` で確認可)
- vitest で:
  - CSV 行のカンマ・ダブルクォートエスケープが正しい
  - xlsx Buffer が生成されサイズ > 0
  - WorkDayRow の集計値が時間計算ロジックと一致
- store_id 省略時は `hanare-all-...` ファイル名で返る
- ヘッダ行が凍結されている (xlsx の views に ySplit:1)
