# task-4007 done_when

- admin で `POST /api/employees` 成功 → DB に追加され employee_stores も更新される
- staff で同じ POST を叩くと 403
- `PATCH /api/employees/:id` で hourly_wage 更新が反映される
- `POST /api/employees/:id/retire` で retire_date がセットされ、`GET /api/employees` のデフォルト (include_retired 無し) では除外される
- POST/PATCH 操作後 audit_logs に対応行が記録される
- `POST /api/employees/:id/pin` で PIN ハッシュが更新される
- 店舗 CRUD も同様に動作する
