# task-4003 done_when

- 状態 off で clock_in → 200, working で clock_in → 409 が返る
- `POST /api/punches` 成功時にメッセージに「お疲れさまです」と HH:MM が含まれる
- `GET /api/punches/me/state` が現在状態を正しく返す
- `GET /api/punches/me/summary` が当月集計を返す
- `tests/unit/time.test.ts` 以下のケースが pass:
  - 通常日勤 (10:00-19:00, 休憩 60 分) → worked=480, overtime=0, night=0
  - 残業日 (10:00-21:00, 休憩 60 分) → worked=600, overtime=120
  - 深夜跨ぎ (20:00-翌03:00, 休憩 0) → worked=420, night=300
  - 休憩複数回が合算される
- 500ms 以内に POST /api/punches がレスポンスする (ローカル単独計測)
- 過去 3 ヶ月分の履歴クエリが返る
