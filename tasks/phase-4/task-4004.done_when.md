# task-4004 done_when

- staff トークンで `GET /api/shifts` を叩くと published のみが返る
- manager で叩くと draft + published 両方が返る
- 同一従業員同日に重複時間帯の POST を試みると 409 が返る
- `/api/shifts/publish` で範囲内 draft が published に変わる
- 公開操作後 `audit_logs` に対応行が追加される
- シフト希望の POST/GET/DELETE が動作する
- vitest で重複検出ロジックの unit テストが pass
