# task-4005 done_when

- staff が `POST /api/corrections` で申請を作成できる
- manager が `POST /api/corrections/:id/approve` で承認 → 対象 punch が更新され audit_logs に before/after が記録される
- `POST /api/corrections/:id/reject` で `review_comment` 必須、欠如時 422
- 承認済み・却下済み申請を再承認しようとすると 409
- admin で `GET /api/audit` がページング付きで返る
- 監査ログを削除する API が存在しない (実装の grep で確認)
- vitest で承認フロー unit テストが pass
